namespace Syracuse;

public enum SaleType
{
    Beginner, Profi, Standart, Pro, Coach,
    Posing, Endo,
    WorkoutProgram,
}

public enum MailType
{
    Success, Awaiting, Failure, Inner,
}

public enum ContactType
{
    Email = 25, Phone = 26, Address = 27
}